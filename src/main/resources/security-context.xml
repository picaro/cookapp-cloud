<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-3.1.xsd">


<global-method-security pre-post-annotations="enabled" />

    <http use-expressions="true" entry-point-ref="restAuthenticationEntryPoint"
            authentication-manager-ref="cookCloudAuthenticationManager">
        <intercept-url pattern="/welcome*" access="ROLE_USER" />
        <intercept-url pattern="/rest/*" access="isAuthenticated()" />
        <intercept-url pattern="/rest/auth/login*" access="isAnonymous()" />
        <intercept-url pattern="/site/*" access="isAuthenticated()" />

        <!--<form-login login-processing-url="/auth/login"-->
                    <!--default-target-url="/rest/auth/success"-->
                    <!--authentication-failure-url="/rest/auth/failure"-->
                    <!--username-parameter="name"-->
                    <!--password-parameter="password" />-->

        <!--<logout invalidate-session="true"-->
                <!--delete-cookies="JSESSIONID, SPRING_SECURITY_REMEMBER_ME_COOKIE"-->
                <!--logout-success-url="/auth/logout" />-->
    </http>

    <authentication-manager id="cookCloudAuthenticationManager">
        <authentication-provider>
            <jdbc-user-service data-source-ref="dataSource"
                               users-by-username-query=
                                       "select email as username, password, enabled from person where email = ?"
                               authorities-by-username-query=
                                       "select p.email as username, ur.role_name as authority from person p, user_role ur where p.id = ur.user_id and p.email = ?"
            />
            <password-encoder hash="md5"/>
        </authentication-provider>
    </authentication-manager>

</beans:beans>
